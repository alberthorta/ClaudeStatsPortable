#pragma once

namespace WebUi {
    // Shared CSS and SVG for both provisioning and webconfig pages.
    // Kept inline so it works in AP mode without internet access.
    extern const char* const STYLES;
    extern const char* const ICON_SVG;
    extern const char* const HEAD_META;
}
